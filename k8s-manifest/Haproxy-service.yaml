apiVersion: v1
kind: Service
metadata:
  name: haproxy-service
spec:
  type: ClusterIP
  selector:
    app: haproxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30081